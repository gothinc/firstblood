#ifndef _FB_STRING_CONFIG_H_

#define _FB_STRING_CONFIG_H_
int fb_del_before(char *s, int del, int len);
char *link_str(char *pre, char *last, char *ret);
int str_pos(char *s, char f);
int str_rpos(char *s, char f);

#endif
